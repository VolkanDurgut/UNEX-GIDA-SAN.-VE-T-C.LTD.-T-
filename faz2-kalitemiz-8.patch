diff --git a/app/globals.css b/app/globals.css
index 57650f0..06e052a 100644
--- a/app/globals.css
+++ b/app/globals.css
@@ -350,8 +350,8 @@ h1 em, h2 em { font-family: var(--font-caveat), cursive; font-weight: 500; color
 
 /* "İşinizi ileri taşırız" — ihracat ağı görseli (gerçek fotoğraf, beyaz
    zeminde kart görünümünde). */
-.quality-map-visual { margin-top: 56px; border-radius: 12px; overflow: hidden; box-shadow: 0 24px 60px rgba(18,23,32,.18); }
-.quality-map-visual img { display: block; width: 100%; height: auto; }
+.page-hero-map-overlay { position: absolute; inset: 0; mix-blend-mode: screen; opacity: .9; pointer-events: none; }
+.page-hero-map-overlay img { object-fit: cover; object-position: center; }
 
 .contact-section, .catalog-section, .related { background: var(--cream); }
 .contact-grid { display: grid; grid-template-columns: .85fr 1.15fr; gap: 100px; }
diff --git a/app/kalitemiz/page.tsx b/app/kalitemiz/page.tsx
index f247ea8..f564015 100644
--- a/app/kalitemiz/page.tsx
+++ b/app/kalitemiz/page.tsx
@@ -1,5 +1,4 @@
 import Link from 'next/link';
-import Image from 'next/image';
 import type { Metadata } from 'next';
 import { ArrowRight, ShieldCheck } from 'lucide-react';
 import { PageHero } from '@/components/page-hero';
@@ -25,6 +24,7 @@ export default function QualityPage() {
         }
         text="Bugünün ihtiyaçlarını anlayan, yarının pazarlarına hazırlanan bir üretim anlayışı."
         image="/quality-lab.jpg"
+        overlayImage="/export-network-map.jpg"
       />
 
       <section className="section-padding quality-copy">
@@ -51,13 +51,6 @@ export default function QualityPage() {
             </div>
           </Reveal>
         </div>
-        <div className="container">
-          <Reveal delay={0.1}>
-            <div className="quality-map-visual">
-              <Image src="/export-network-map.jpg" alt="" width={1600} height={873} />
-            </div>
-          </Reveal>
-        </div>
       </section>
 
       <section className="statement-band">
diff --git a/components/page-hero.tsx b/components/page-hero.tsx
index c8f7055..d5eb38f 100644
--- a/components/page-hero.tsx
+++ b/components/page-hero.tsx
@@ -11,6 +11,7 @@ export function PageHero({
   image,
   images,
   visual,
+  overlayImage,
   theme = 'dark',
   flourDust = false,
 }: {
@@ -22,6 +23,11 @@ export function PageHero({
   images?: string[];
   /** Fotoğraf yerine tamamen özel bir görsel bileşen (ör. harita, grafik). Verilirse `image`/`images` yok sayılır. */
   visual?: ReactNode;
+  /** Ana fotoğrafın ÜZERİNE, "screen" blend modu ile bindirilen ikinci bir
+   *  görsel — koyu/siyah zeminli grafikleri (ör. ışıklı harita) şeffaf
+   *  gibi göstermek için idealdir: siyah otomatik kaybolur, sadece parlak
+   *  çizgi/noktalar fotoğrafın üstünde belirir. */
+  overlayImage?: string;
   /** 'dark' (varsayılan): mevcut lacivert/fotoğraf üstü beyaz metin.
    *  'light': beyaz zemin, lacivert başlık — iki koyu bölümün art arda
    *  gelmesini istemediğimiz sayfalarda (örn. Hakkımızda) kullanılır. */
@@ -38,6 +44,11 @@ export function PageHero({
       ) : image ? (
         <Image src={image} alt="" fill priority sizes="100vw" />
       ) : null}
+      {overlayImage ? (
+        <div className="page-hero-map-overlay">
+          <Image src={overlayImage} alt="" fill sizes="100vw" />
+        </div>
+      ) : null}
       <div className="page-hero-shade" />
       {flourDust ? <FlourDust /> : null}
       <div className="container page-hero-content">
