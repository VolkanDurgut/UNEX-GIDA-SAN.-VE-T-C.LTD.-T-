diff --git a/app/globals.css b/app/globals.css
index 654ed92..4ae46ed 100644
--- a/app/globals.css
+++ b/app/globals.css
@@ -169,7 +169,7 @@ h1 em, h2 em { font-family: var(--font-caveat), cursive; font-weight: 500; color
 .intro-heading { text-align: center; max-width: 920px; margin: 0 auto 64px; }
 .intro-heading h2 { font-size: clamp(28px, 4.2vw, 60px); white-space: normal; }
 .eyebrow.center { display: flex; justify-content: center; }
-.about-story h2, .quality-copy h2 { font-size: clamp(40px, 5vw, 68px); color: var(--navy); font-weight: 600; letter-spacing: -.045em; }
+.quality-copy h2 { font-size: clamp(40px, 5vw, 68px); color: var(--navy); font-weight: 600; letter-spacing: -.045em; }
 .intro-heading h2 { color: var(--navy); font-weight: 600; letter-spacing: -.045em; }
 .section-heading-centered { display: flex; justify-content: center; margin-bottom: 42px; }
 .intro-copy { max-width: 480px; }
@@ -277,18 +277,32 @@ h1 em, h2 em { font-family: var(--font-caveat), cursive; font-weight: 500; color
 .page-hero-content { position: relative; }
 .page-hero h1 { font-size: clamp(46px, 7vw, 88px); font-weight: 600; letter-spacing: -.065em; line-height: .95; }
 .page-hero h1 span { color: var(--gold); }
-.page-hero h1 em { color: #f4b18d; }
+.page-hero h1 em { color: var(--gold); }
+.page-hero .eyebrow span { background: var(--gold); }
 .page-hero-content > p:last-child { max-width: 410px; font-size: 15px; line-height: 1.65; color: rgba(255,255,255,.74); margin-top: 20px; }
 
-.about-story, .quality-copy { background: var(--cream); }
+.quality-copy { background: var(--cream); }
 .about-grid, .quality-layout { align-items: start; }
-.about-story h2, .quality-copy h2 { font-size: clamp(38px, 5vw, 64px); }
-
-.stats { background: var(--navy); color: #fff; padding: 68px 0; }
-.stats-grid { display: grid; grid-template-columns: repeat(3, 1fr); }
-.stat { padding-left: 28px; border-left: 1px solid rgba(255,255,255,.2); display: flex; flex-direction: column; gap: 7px; }
-.stat strong { font-family: 'Poppins', sans-serif; font-size: 68px; line-height: 1; color: #f4ad87; letter-spacing: -.07em; }
-.stat span { font-size: 11px; letter-spacing: .12em; text-transform: uppercase; color: rgba(255,255,255,.62); }
+.quality-copy h2 { font-size: clamp(38px, 5vw, 64px); }
+
+/* ===== HAKKIMIZDA: "Birlikte büyüyen" + İstatistikler — tek, kesintisiz
+   lacivert blok. FlourDust bu bloğun tamamını (başlık alanı + istatistik
+   şeridi) kaplayacak şekilde bir kez, en dışta render ediliyor. ===== */
+.about-navy-block { position: relative; overflow: hidden; background: var(--navy); color: #fff; }
+.about-navy-block h2 { font-size: clamp(38px, 5vw, 64px); color: #fff; font-weight: 600; letter-spacing: -.045em; }
+.about-navy-block .eyebrow { color: #fff; }
+.about-navy-block .body-copy p { color: rgba(255,255,255,.78); }
+.about-navy-block > .section-padding { position: relative; z-index: 2; }
+.about-navy-block > .stats { position: relative; z-index: 2; }
+
+.stats { padding: 80px 0; }
+.stats-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 40px; }
+.stat { padding-left: 28px; border-left: 1px solid rgba(255,255,255,.18); display: flex; flex-direction: column; gap: 12px; }
+.stat-icon { color: var(--gold); }
+.stat-value { display: flex; align-items: baseline; gap: 5px; }
+.stat-value strong { font-family: 'Poppins', sans-serif; font-size: 56px; line-height: 1; color: #fff; letter-spacing: -.03em; }
+.stat-suffix { font-family: 'Poppins', sans-serif; font-size: 26px; font-weight: 600; color: var(--gold); }
+.stat-label { font-size: 11px; letter-spacing: .14em; text-transform: uppercase; color: rgba(255,255,255,.6); }
 
 .cta-block { background: linear-gradient(135deg, var(--navy), #16223f); position: relative; color: #fff; padding: 105px 0; }
 .cta-block .container { position: relative; }
@@ -415,7 +429,7 @@ input:focus, textarea:focus { border-color: var(--gold); }
   .hero-script { font-size: 34px; }
   .hero-copy { font-size: 14px; }
   .section-heading { align-items: flex-start; gap: 20px; flex-direction: column; }
-  .intro-section h2, .about-story h2, .quality-copy h2 { font-size: 43px; }
+  .intro-section h2, .about-navy-block h2, .quality-copy h2 { font-size: 43px; }
   .together h2 { font-size: 70px; }
   .product-grid { grid-template-columns: 1fr; gap: 30px; }
   .product-visual { height: 345px; }
diff --git a/app/hakkimizda/page.tsx b/app/hakkimizda/page.tsx
index 10f61b4..bbbe086 100644
--- a/app/hakkimizda/page.tsx
+++ b/app/hakkimizda/page.tsx
@@ -3,6 +3,7 @@ import type { Metadata } from 'next';
 import { ArrowRight, Handshake } from 'lucide-react';
 import { PageHero } from '@/components/page-hero';
 import { Stats } from '@/components/stats';
+import { FlourDust } from '@/components/flour-dust';
 import { Reveal } from '@/components/motion-primitives';
 
 export const metadata: Metadata = {
@@ -27,36 +28,39 @@ export default function AboutPage() {
         image="/hero-photo.jpg"
       />
 
-      <section className="section-padding about-story">
-        <div className="container about-grid">
-          <Reveal direction="left">
-            <div>
-              <p className="eyebrow">UNEX HAKKINDA</p>
-              <h2>
-                Birlikte büyüyen
-                <br />
-                <em>30+ yıllık hikaye.</em>
-              </h2>
-            </div>
-          </Reveal>
-          <Reveal direction="right" delay={0.1}>
-            <div className="body-copy">
-              <p>
-                Uluslararası buğday unu ve emtia piyasasında 30 yılı aşkın tecrübemizle 1.000.000 tonun üzerinde
-                buğday unu elleçledik. Kalite, dinamizm ve profesyonellik gibi temel değerlerimiz, Türkiye&apos;nin
-                en büyük buğday unu ihracatçılarından biri olmamızı sağlamıştır.
-              </p>
-              <p>
-                Bugüne kadar 50&apos;den fazla ülkeye yaptığımız ihracat sayesinde kaliteli buğday unlarımızı
-                dünyanın her köşesinden müşterilerimize ulaştırmanın gururunu yaşıyoruz. Müşterilerimize
-                uzmanlığımızı ve deneyimimizi sunarak onlarla stratejik bir ortak olmaya çalışıyoruz.
-              </p>
-            </div>
-          </Reveal>
+      <section className="about-navy-block">
+        <FlourDust />
+        <div className="section-padding">
+          <div className="container about-grid">
+            <Reveal direction="left">
+              <div>
+                <p className="eyebrow">UNEX HAKKINDA</p>
+                <h2>
+                  Birlikte büyüyen
+                  <br />
+                  <em>30+ yıllık hikaye.</em>
+                </h2>
+              </div>
+            </Reveal>
+            <Reveal direction="right" delay={0.1}>
+              <div className="body-copy">
+                <p>
+                  Uluslararası buğday unu ve emtia piyasasında 30 yılı aşkın tecrübemizle 1.000.000 tonun üzerinde
+                  buğday unu elleçledik. Kalite, dinamizm ve profesyonellik gibi temel değerlerimiz,
+                  Türkiye&apos;nin en büyük buğday unu ihracatçılarından biri olmamızı sağlamıştır.
+                </p>
+                <p>
+                  Bugüne kadar 50&apos;den fazla ülkeye yaptığımız ihracat sayesinde kaliteli buğday unlarımızı
+                  dünyanın her köşesinden müşterilerimize ulaştırmanın gururunu yaşıyoruz. Müşterilerimize
+                  uzmanlığımızı ve deneyimimizi sunarak onlarla stratejik bir ortak olmaya çalışıyoruz.
+                </p>
+              </div>
+            </Reveal>
+          </div>
         </div>
-      </section>
 
-      <Stats />
+        <Stats />
+      </section>
 
       <section className="statement-band">
         <Reveal className="container statement-inner">
diff --git a/components/stats.tsx b/components/stats.tsx
index f0155a9..f64ffaa 100644
--- a/components/stats.tsx
+++ b/components/stats.tsx
@@ -2,16 +2,12 @@
 
 import { motion, useInView, useMotionValue, useTransform, animate } from 'framer-motion';
 import { useEffect, useRef } from 'react';
+import { CalendarClock, Wheat, Globe } from 'lucide-react';
 import { stats } from '@/lib/data';
 
-function splitNumber(raw: string): { number: number; suffix: string } {
-  const match = raw.match(/^(\d+)(.*)$/);
-  if (!match) return { number: 0, suffix: raw };
-  return { number: Number(match[1]), suffix: match[2] };
-}
+const ICONS = [CalendarClock, Wheat, Globe];
 
-function Counter({ value }: { value: string }) {
-  const { number, suffix } = splitNumber(value);
+function Counter({ value }: { value: number }) {
   const ref = useRef<HTMLSpanElement>(null);
   const inView = useInView(ref, { once: true, amount: 0.6 });
   const count = useMotionValue(0);
@@ -19,15 +15,14 @@ function Counter({ value }: { value: string }) {
 
   useEffect(() => {
     if (inView) {
-      const controls = animate(count, number, { duration: 1.4, ease: 'easeOut' });
+      const controls = animate(count, value, { duration: 1.4, ease: 'easeOut' });
       return () => controls.stop();
     }
-  }, [inView, number, count]);
+  }, [inView, value, count]);
 
   return (
     <strong ref={ref}>
       <motion.span>{rounded}</motion.span>
-      {suffix}
     </strong>
   );
 }
@@ -36,20 +31,31 @@ export function Stats() {
   return (
     <section className="stats">
       <div className="container stats-grid">
-        {stats.map(([number, label], index) => (
-          <motion.div
-            className="stat"
-            key={label}
-            initial={{ opacity: 0, y: 20 }}
-            whileInView={{ opacity: 1, y: 0 }}
-            viewport={{ once: true, amount: 0.4 }}
-            transition={{ duration: 0.6, delay: index * 0.12 }}
-          >
-            <Counter value={number} />
-            <span>{label}</span>
-          </motion.div>
-        ))}
+        {stats.map(({ value, suffix, label }, index) => {
+          const Icon = ICONS[index];
+          return (
+            <motion.div
+              className="stat"
+              key={label}
+              initial={{ opacity: 0, y: 20 }}
+              whileInView={{ opacity: 1, y: 0 }}
+              viewport={{ once: true, amount: 0.4 }}
+              transition={{ duration: 0.6, delay: index * 0.12 }}
+            >
+              {Icon ? (
+                <div className="stat-icon">
+                  <Icon size={22} />
+                </div>
+              ) : null}
+              <div className="stat-value">
+                <Counter value={value} />
+                <span className="stat-suffix">{suffix}</span>
+              </div>
+              <span className="stat-label">{label}</span>
+            </motion.div>
+          );
+        })}
       </div>
     </section>
   );
-}
\ No newline at end of file
+}
diff --git a/lib/data.ts b/lib/data.ts
index 148884a..3971f3c 100644
--- a/lib/data.ts
+++ b/lib/data.ts
@@ -102,8 +102,8 @@ export const values = [
 
 export const suppliers = ['ADM', 'Bunge', 'Cargill', 'Louis Dreyfus', 'Glencore', 'Viterra', 'Olam'];
 
-export const stats: [string, string][] = [
-  ['30+', 'Yıl deneyim'],
-  ['1M+', 'Ton buğday'],
-  ['50+', 'Ülkeye ihracat'],
+export const stats: { value: number; suffix: string; label: string }[] = [
+  { value: 30, suffix: '+', label: 'Yıl deneyim' },
+  { value: 1, suffix: 'M+', label: 'Ton buğday' },
+  { value: 50, suffix: '+', label: 'Ülkeye ihracat' },
 ];
\ No newline at end of file
